/*
this file is for education purposes 
if someone send the file like that please don't accept the submission 
*/

#include "id.h"
#include <stdio.h>
int main(){
   int a=4,b=5; 
   printf("\n(%d+%d)² = %d\n",a,b,first-id(a,b));
   printf("\n(%d-%d)² = %d\n",a,b,second_id(a,b));
   printf("\n(%d+%d)(%d-%d) = %d\n",a,b,a,b,third_id(a,b));
}
